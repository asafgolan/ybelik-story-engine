# generate_scene worker (pipeline stage 0) — see generate/README.md
# repo-root .env is the SINGLE source for these; generate_scene.py auto-loads it
# via load_env_file() (the real environment wins over the file). .env is gitignored.
GENERATE_ENDPOINT=https://ybelik-generate.<account>.workers.dev
GENERATE_TOKEN=mint-one-per-consumer-via-wrangler-secret
